name: Reusable workflow example

on:
  workflow_run:
    

jobs:
  example_job:
    name: Pass input and secrets to my-action
    runs-on: [ self-hosted ]
    steps:
      - uses: ./.github/workflows-templates/dummy-ci.yml
        
