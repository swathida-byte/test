
name: Call a reusable workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches:
      - main

jobs:
  call-workflow:
    runs-on: self-hosted

    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Workflow
      uses: re.yml@v5
      

  

  


    

